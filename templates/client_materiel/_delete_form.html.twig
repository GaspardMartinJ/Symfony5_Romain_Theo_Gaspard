<form method="post" action="{{ path('app_client_materiel_delete', {'id': client_materiel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_materiel.id) }}">
    <button class="btn">Delete</button>
</form>
