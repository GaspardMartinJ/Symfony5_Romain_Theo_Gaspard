{% extends 'base.html.twig' %}

{% block title %}ClientMateriel{% endblock %}

{% block body %}
    <h1>ClientMateriel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client_materiel.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_materiel_index') }}">back to list</a>

    <a href="{{ path('app_client_materiel_edit', {'id': client_materiel.id}) }}">edit</a>

    {{ include('client_materiel/_delete_form.html.twig') }}
{% endblock %}
