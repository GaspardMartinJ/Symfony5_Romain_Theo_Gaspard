{% extends 'base.html.twig' %}

{% block title %}Magasin index{% endblock %}

{% block body %}
    <h1>Magasin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Ville</th>
                <th>Num_Siret</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for magasin in magasins %}
            <tr>
                <td>{{ magasin.id }}</td>
                <td>{{ magasin.Nom }}</td>
                <td>{{ magasin.Ville }}</td>
                <td>{{ magasin.NumSiret }}</td>
                <td>
                    <a href="{{ path('app_magasin_show', {'id': magasin.id}) }}">show</a>
                    <a href="{{ path('app_magasin_edit', {'id': magasin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_magasin_new') }}">Create new</a>
    <br>
    <a href="{{ path('app_index') }}">Back to home page</a>
{% endblock %}
